# 更新日志

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 风格，版本号遵循语义化版本规范。

## [1.0.5] - 2026-08-27

新增 Tauri 2.0 原生免安装绿色版客户端 (.exe)、全面优化跨平台字体与字号比例、等级双行排版与预设精简。

### Added

- **Tauri 2.0 原生单文件绿色客户端**：新增纯单文件免安装 Windows 桌面客户端（`剑网3掉落工坊-v1.0.5.exe`，仅约 8MB），不写注册表、零环境依赖，双击即开，内存占用低至 30~50MB。
- **GitHub Actions 自动化流水线**：配置云端自动化编译工作流，打标签发布时全自动构建多架构二进制与 Release 发布。

### Changed

- **跨平台字体栈与视觉字号升级**：Windows 端默认采用华为鸿蒙黑体（HarmonyOS Sans SC），苹果端采用苹方（PingFang SC），全局字号与按钮尺寸全面调优放大，消除眯眼感与字体偏小问题。
- **副本版本与等级双行结构化排版**：左侧侧边栏版本分组重构为双行独立排版（版本名称居上、等级居下），彻底解决两位数与三位数自然折行不一致的视觉抖动。
- **精简推荐预设与提示文案**：预设按钮与 Toast 提示文案同步精简为「推荐预设」，鼠标悬停保留完整气泡说明。

## [1.0.4] - 2026-08-27

增强数据安全保护、优化物品移除与批量管理体验、提升跨平台与离线稳定性。

### Fixed

- **配置加载与安全保护**：修复极端异常（如数据损坏或存储受限）下可能导致已有配置被重置的隐患；异常时自动启用只读保护并隔离备份旧数据，杜绝误覆盖。
- **彻底清理已删除物品**：修复移出自选库的物品仍可能残留在导出配置中的问题，确保“界面移除后导出文件绝对不包含”。
- **优化官方推荐物品批量管理**：移出官方推荐保护列表后的物品，可正常响应全选、批量出售等快捷策略。
- **存储异常提醒**：当浏览器存储空间不足或保存失败时，提供清晰的界面提示。
- **文件导入防护**：导入超大文件时增加前置安全校验，避免浏览器卡顿或内存溢出。
- **界面细节与文字纠偏**：统一修正资料片名称为官方正名「丝路风语」，优化低性能模式动效，全平台单文件离线版体验更稳定。

## [1.0.3] - 2026-08-25

将插件默认数据（21 项推荐保护与 19 项默认出售）注册至用户自定义物品库，提升前端可见性与可编辑性。

### Changed

- **默认数据入驻用户自定义库**：初始工作区将 21 项推荐保护特效武器与 19 项默认出售杂物直接注册入 `customItems`，在左侧【✨ 用户自定义】及未选择任何秘境时即时全量展示，支持自由勾选、改动与移除。

## [1.0.2] - 2026-08-25

修正初始跳过拾取占位符配置。

### Fixed

- **初始跳过拾取占位移除**：移除用户本地占位测试项 `金叶子` 的默认跳过拾取标记，保持插件默认初始状态为「无跳过拾取过滤项」，仅保留自动出售 19 项与推荐保护 21 项。

## [1.0.1] - 2026-08-25

合入官方插件默认初始配置对齐补丁。

### Changed

- **官方插件默认初始配置全量合入**：将 `自动拾取默认配置.us.jx3dat`（跳过拾取：金叶子）与 `自动售卖默认配置.us.jx3dat`（自动出售：19 项换金杂物；推荐保护：21 项特效武器与珍品）作为新工作区的初始默认配置，实现与游戏插件默认行为的 100% 深度对齐。
- **目录元数据扩展**：目录系统自动补全插件默认出售与过滤物品，支持在任意界面统一检索与管理。

## [1.0.0] - 2026-08-25

剑网3掉落工坊 1.0.0 正式版。

### Added

- **合一配置导出与导入**：支持将【跳过拾取】（`MY_GKPLoot`）与【自动出售 / 保护不出售】（`MY_AutoSell`）合并为单一 `.us.jx3dat` 配置文件导出，游戏内点击一次导入即可全部生效。
- **顶栏高定操作中心**：重构导出交互为右上角高定分段按钮（Split Button）与微光下拉菜单，支持一键导出合一配置、细分独立导出以及实时动态撤销变更。
- **自定义范围管理**：左侧副本树新增【✨ 用户自定义】顶层节点，集中展示与管理所有用户自行添加的自定义物品。
- **正统剑三水墨品牌标识**：设计并上线正统大唐水墨狂草“剑”字与朱砂“叁”印章 Logo，全平台网页与单文件离线版同步嵌入高质感 Favicon。
- **双版本状态与署名呈现**：左上角品牌区展示软件版本（`v1.0.0`）、数据库版本（`Data.丝路风语-260823`）及作者署名（`by 凌千羽·龙争虎斗`）。

### Changed

- **全面纯净化界面用语**：彻底消除“manifest”“IndexedDB”“SHA-256”“保护基线”“快照”等开发技术术语，所有弹窗与提示全部改写为自然亲切的玩家通俗语言。
- **侧边栏与布局极致减负**：移除右下角狭窄浮动小黑框与侧边栏底部占用高度的注脚卡片，左侧副本树实现 100% 全高度顺畅滚动。
- **像素级标签对齐**：软件版本与数据版本药丸外框达成严格像素级高度（22px）与基线对齐。

### Fixed

- 修复用户自定义范围节点勾选框溢出遮挡标题的样式问题。
- 修复批量应用与导入时版本指纹未能置于数据首位的问题。

## [0.3.0] - 2026-08-25

分类体系重构与目录缓存机制优化版本。

### Added

- 基于 JX3BOX 上游 `TypeLabel` 建立多级规则分类体系，新增“装备兑换牌”“特殊掉落”“宠物”“大铁”“小铁”“家具”“小附魔”“大附魔”等实用大类，物品未分类数从万余条精简至 102 条。
- 引入纯 TypeScript 分类器模块 `web/src/catalog/classification.ts` 与规则配置 `type-label-rules.json`，为“其他”标签和空标签提供结构解析、宠物优先、铁类与兑换牌等多级受控识别链。
- 新增离线目录重分类脚本 `npm run reclassify:catalog`，支持本地幂等更新快照与 manifest。
- 补充 49 项分类器单测与目录选择策略单元测试，测试用例扩充至 76 项。

### Changed

- 界面筛选器与领域层完整适配新增物品分类枚举。
- 同步数据抓取脚本（`crawl-catalog.mjs`）的分类逻辑，确保在线抓取、离线重分类和运行时分类三链统一。
- 在线静态目录快照与单文件内嵌目录升级为 `20260823-011b26367022` 版本。

### Fixed

- 修复浏览器 IndexedDB 旧目录覆盖策略：重构 `selectCatalogSnapshot`，改用版本时间与内容 Hash 双重判定，彻底解决旧缓存无条件遮蔽代码内置新目录的 Bug，同时保留用户手动导入更新目录的路径。
- 在线目录获取增加 `cache: 'no-store'`，避免同名静态 JSON 被浏览器强缓存。

### Data

- 目录快照版本升级为 `20260823-011b26367022`（contentHash: `011b26367022797495297bdf09b943d29acb5f6cd6b480b95128757ac25edce1`）。
- 精确去重物品 16,571 项中：装备 12,488 项、装备兑换牌 3,442 项、宠物 229 项、书籍 107 项、未分类 102 项、家具 73 项、特殊掉落 71 项、材料 29 项、大铁 8 项、小铁 8 项、小附魔 8 项、大附魔 6 项。

### Known Limitations

- 剩余 102 条未分类物品主要为缺乏明确元数据与规则证据的历史杂项，后续待补充证据或规则后逐步归类，不进行无依据猜测。
- 消耗品、任务物品、货币/兑换等预留大类暂未映射，待后续收集用户使用习惯后扩充。

## [0.2.1] - 2026-08-25

团队秘境排序与响应式适配补丁版本。

### Changed

- 优化等级分组下的秘境排序：10 人和 25 人团队秘境按 `MapID` 降序排列，最新团队副本优先展示在最上方；5 人秘境与其他秘境保持既有顺序。
- 精简副本树节点信息，移除已有等级组上下文下的重复资料片名称，优化页面头部文案。
- 增强响应式布局与触控体验，完善桌面、平板及移动窄屏（≤900px、≤600px）下的字号（CSS 变量）、触控热区、卡片间距与自适应折行。

### Fixed

- 修复较小视口下部分筛选器和操作按钮宽度溢出与触控不便的问题。

### Test

- 补充 10 人/25 人团队秘境按 MapID 降序排列的单元测试用例。

## [0.2.0] - 2026-08-25

第二版体验与启动性能优化版本。

### Added

- 按游戏等级重组左侧版本导航，固定显示 130、120、110、100、95、90、80、70 八个等级组；等级内按 5 人、10 人和 25 人秘境及实际存在的难度分类。
- 增加“快速配置”，支持低等级装备自动出售、当前范围自动出售、当前范围保护和清除出售策略，并统一走差异预览、确认与撤销。

### Changed

- 将“自动出售”和“保护不出售”合并为“出售策略”的“未处理 / 自动出售 / 保护不出售”三选一控件；“跳过拾取”保持独立。
- 使用响应式字号、侧栏和触控尺寸，改善桌面、平板和窄屏显示密度。

### Fixed

### Performance

- 在线版将完整目录改为首屏水合后异步加载，客户端主页面脚本从约 8 MB 降到约 61 KB；离线版仍把目录内嵌在单文件中，目录不重复打进 JavaScript。
- 本地开发仅加载 vinext，托管构建插件延迟到生产构建；启动脚本默认使用生产服务器，开发模式仍可用 `npm run dev`。
- 将完整目录的物品索引建立延后到首屏空闲时间，减少首次渲染阻塞；批量矩阵的类别统计改为缓存计算。

### Data

- 新增版本等级与秘境难度的类型化映射；未知版本/其他人数副本保留在安全回退组，不丢失目录数据。

### Known Limitations

- 物品筛选器目前仍沿用上游目录类别；更贴近日常使用的分类口径（尤其是消耗品、货币/兑换和其他）将在下一阶段确认映射后调整。
- 在线版首屏已先加载轻量页面壳，但首次异步载入完整目录仍受本地网络和浏览器缓存影响。
- 未知版本或暂未匹配人数/难度的副本会保留在安全回退组，待后续补充数据映射。

## [0.1.1] - 2026-08-25

品牌与兼容性修订版本。

### Changed

- 产品中文名统一为“剑网3掉落工坊”，同步更新页面标题、离线页面、README、开发文档、发版文档和工作区备份提示。
- 新生成的 `.us.jx3dat` 指纹使用 `『剑网3掉落工坊』`；内部仓库 slug、存储 key 和文件格式保持兼容。

### Fixed

- 导入器继续识别旧版 `JX3 Loot Forge` 指纹、跳过拾取标记和自动出售标记，已有 v0.1.0 配置无需重建。

## [0.1.0] - 2026-08-24

首个可用版本，面向剑网 3 旗舰端（`client=std`）。

### Added

- 内置旗舰端完整副本、Boss 与掉落目录，支持按版本、副本、难度和 Boss 选择范围，并可按类别、品质、装备部位、装等和状态筛选。
- 为物品提供“跳过拾取”“自动出售”“保护不出售”三状态；自动出售与保护不出售严格互斥，跳过拾取独立生效。
- 支持按物品类型批量应用规则，应用前预览差异，支持自定义物品、一次应用后的撤销和逐项调整。
- 支持自定义物品名称，以及已有 `.us.jx3dat` 配置的安全导入，提供合并/替换预览。
- 支持工作区本地持久化、备份与恢复，刷新或重启后保留用户配置。
- 支持构建单个 `index.html` 离线版，双击即可使用，默认不依赖服务器或网络。

### Changed

- 首次启动建立受保护物品基线，并将自定义物品排除在目录批量规则之外。
- 导出时可分别或同时生成跳过拾取与自动出售配置文件，文件后缀固定为 `.us.jx3dat`。

### Data

- 目录快照版本：`20260823-47ec717b1065`。
- 覆盖 254 个 MapID、981 个 Boss、43,033 条掉落记录和 16,571 个精确去重物品名称。
- 导出文件使用 CP936/GBK 编码、无 BOM、单行格式，符合游戏导入要求。

### Known Limitations

- 界面字号和模块布局的响应式适配仍需优化，部分窗口尺寸下显示密度偏高、文字偏小。
- 自动出售与保护不出售在当前界面中仍以独立模块呈现，虽然领域层已互斥，尚未完成合并为统一处理方式入口。
- 版本分类尚未按等级归并，同一等级可能显示多个版本分类。
- 首屏仍可能同步加载较大的完整目录，启动和首次可操作时间的优化留待后续版本。
