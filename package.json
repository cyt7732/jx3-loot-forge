{
  "name": "jx3-loot-forge-root",
  "version": "1.2.2",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "npm --prefix web run build:web",
    "build:web": "npm --prefix web run build:web",
    "build:offline": "npm --prefix web run build:offline",
    "package:artifacts": "node web/scripts/package-artifacts.mjs"
  }
}