# 剑网3掉落工坊 - 项目开发规则 (Project Rules)

## 数据包版本命名规范 (Catalog Data Versioning Rule)

每次抓取、构建及更新数据包时，必须严格遵循以下版本命名规则：

### 1. 命名格式模板
```text
data.<客户端>_<资料片名>_v<赛季序号>_<YYMMDD>
```

### 2. 各元素说明与规范
- **`data.`**：固定前缀。
- **`<客户端>`**：
  - `旗舰`：对应正式服 / 旗舰版（API `client=std`）
  - `缘起`：对应怀旧服 / 缘起版（API `client=origin`）
- **`<资料片名>`**：当前客户端数据包所覆盖的最新资料片全称（如 `丝路风语`、`苍生铸世`、`剑胆琴心` 等）。
- **`v<赛季序号>`**：当前资料片生命周期内的赛季版本序号（大资料片通常包含 4 个赛季，随着新副本和赛季更迭递增，如 `v1`、`v2`、`v3`、`v4`；资料片更迭时重置为 `v1`）。
- **`<YYMMDD>`**：数据包爬取或发布的年月日时间戳（6 位，如 `260830`、`260930`）。

### 3. 命名标准示例
- **当前旗舰服（第4赛季）**：`data.旗舰_丝路风语_v4_260830`
- **下赛季旗舰服（新资料片首赛季）**：`data.旗舰_苍生铸世_v1_260930`
- **缘起怀旧服（当前当期赛季）**：`data.缘起_剑胆琴心_v1_260917`

### 4. 前端与构建联动要求
- 界面顶部徽章及数据版本标识应动态展示当前选定客户端的数据包完整标识。
- 数据包 manifest 及抓取脚本输出的元数据中，必须包含此标准化版本字段。
